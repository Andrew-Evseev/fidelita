name: fidelita
description: "Система лояльности для салонов красоты и косметологических клиник"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI и навигация
  provider: ^6.1.1
  google_fonts: ^6.1.0
  
  # Локальное хранение данных
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/